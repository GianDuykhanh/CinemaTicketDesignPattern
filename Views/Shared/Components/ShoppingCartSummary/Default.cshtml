@model int;

@if (Model > 0)
{
    <li class="nav-item">
        <a class="nav-link cart-nav-link" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4"></i>
            <span class="cart-badge">@Model</span>
        </a>
    </li>
}